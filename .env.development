# Development environment configuration for vitest-mcp
# This file allows running the MCP server on its own repository for testing since the server prevents running on itself

# Enable development mode to bypass self-protection checks
VITEST_MCP_DEV_MODE=true

# Optional: Enable debug logging
# VITEST_MCP_DEBUG=true